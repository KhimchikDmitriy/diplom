<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand animator" href="/">Аниматор</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Переключатель навигации"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" href="/oursWorks">Наши работы</a>
        </li>
        <li class="nav-item">
          <button
            type="button"
            class="nav-link active"
            data-bs-toggle="modal"
            data-bs-target="#feedback"
          >
            Связаться с поддержкой
          </button>
        </li>
        <% if (locals.user) { %>
        <li class="nav-item">
          <% if (role === "admin") { %>
          <a class="nav-link active" href="/chat"> Написать в ответ</a>
          <% } else {%>
          <a class="nav-link active" href="/chat"> Написать нам</a>
          <% } %>
        </li>
        <% } %>
        <li class="nav-item">
          <% if (locals.user) { %>
          <a class="nav-link active" href="/logout">Выйти</a>
          <% } else { %>
          <button
            class="nav-link active"
            data-bs-toggle="modal"
            data-bs-target="#login"
          >
            Войти или зарегистрироваться
          </button>
          <% } %>
        </li>
      </ul>
    </div>
  </div>
</nav>
<style>
  @font-face {
    font-family: "Sunday";
    src: url("./fonts/fonts_Sunday/sunday.eot");
    src: url("./fonts/fonts_Sunday/sunday.eot?#iefix")
        format("embedded-opentype"),
      url("./fonts/fonts_Sunday/sunday.woff2") format("woff2"),
      url("./fonts/fonts_Sunday/sunday.woff") format("woff"),
      url("./fonts/fonts_Sunday/sunday.ttf") format("truetype"),
      url("./fonts/fonts_Sunday/sunday.svg#sunday") format("svg");
  }
  .animator {
    font-family: "Sunday";
  }
</style>
