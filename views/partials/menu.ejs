<nav class="navbar navbar-expand navbar-light bg-light justify-content-end">
  <div class="nav navbar-nav">
    <% if (locals.user) { %>
    <div id="menu">
      <%# меню пользователей выполнивших вход%>
      <span class="name"><%= user.email %> </span> <%# user.email для браузера
      то, что пользователь послал в requeste%>
      <a class="btn btn-outline-success" href="/new" aria-current="page"
        >Создать пост<span class="visually-hidden">(current)</span></a
      >
      <a class="btn btn-outline-success" href="/logout">Выйти</a>
    </div>
    <% } else { %>
    <div class="" id="menu">
      <%# //меню анонимных пользователей %>
      <a class="btn btn-outline-success" href="/login" aria-current="page"
        >Войти<span class="visually-hidden">(current)</span></a
      >
      <a class="btn btn-outline-success" href="/register">Зарегистрироваться</a>
    </div>
    <% } %>
  </div>
</nav>
